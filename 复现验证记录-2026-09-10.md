# 复现包验证记录（2026-09-10）

状态：已完成干净目录实跑验证。执行人：编排智能体（受作者授权执行）；结论：42/42 测试通过，四组模型输出复现冻结基准。

## 运行环境（实测）

- Python 3.14.7（CPython, Windows amd64）
- numpy 2.5.3（仅 selective_compression_validation 需要；其余三组纯标准库）
- 操作系统：Windows

## 执行步骤

1. 将 staging/reproduction/ 复制到独立干净目录：%TEMP%\dcs-repro-run-20260910（排除 __pycache__）
2. 安装依赖：pip install numpy（numpy 2.5.3）
3. 运行测试：python -m unittest discover -s tests -p "test_*.py"
   - 结果：Ran 42 tests, OK
   - 分项：cpir_exact 12/12；cpir_approximate 9/9；selective_compression_validation 10/10；cross_scale_interface 11/11
4. 运行四组模型脚本（cpir_exact.py、cpir_approximate.py、run_model.py、run_model.py）：全部 rc=0
5. 对比新生成 results.json 与 expected_results/ 冻结基准：
   - cpir_exact：精确一致
   - cpir_approximate：精确一致
   - cross_scale_interface：精确一致
   - selective_compression_validation：34 处浮点尾数差异，全部在 1e-15 量级（最大 delta 约 3.3e-15）；其回归测试 assert_nested_close 以 1e-12 容差比较（原项目为跨 BLAS/LAPACK 构建设计），判定通过

## 结论与口径

- 复现包可在干净目录从零复现论文有限模型数值结果。
- selective_compression_validation 依赖 numpy 特征分解，不同 BLAS/LAPACK 构建存在最后 1e-15 位尾数差异，属该组测试已设计的容差范围（1e-12），不是复现失败。
- README-reproduce.md 已填实环境、步骤、测试数与对比命令；SHA256SUMS.txt 已按最终文件集重新生成（37 个文件）。

## 遗留说明

- 本记录基于当前 Python/numpy 版本；若发布时声明"可复现"，建议在 Zenodo 记录中注明测试环境版本。
- 干净运行目录保留在 %TEMP%\dcs-repro-run-20260910，可复核。
